# ─────────────────────────────────────────────────────────────────────────────
# FasalX Test Suite Dependencies
# Install with:  pip install -r tests/requirements-test.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Test framework ────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# ── HTTP mocking (for httpx) ─────────────────────────────────────────────────
pytest-httpx>=0.30

# ── Application dependencies needed at import time ───────────────────────────
# (Backend)
fastapi>=0.110
httpx>=0.27          # async HTTP client used by services
motor>=3.3           # async MongoDB driver
redis>=5.0           # asyncio Redis
pydantic>=2.0
pydantic-settings>=2.0
firebase-admin>=6.4
uvicorn>=0.29

# (Timeline Service)
arq>=0.25            # background task queue
